import { test } from "@playwright/test";

test.skip("Etape 4 - créer un composant React / Svelte / Vue permettant la navigation entre les pages", () => {
  test.skip("Pas de test nécessaire ici 😁. step3.spec.ts devrait suffire 😉", () => {});
});
